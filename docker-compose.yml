services:
  testing-api:
    build: .
    container_name: testing-api
    restart: unless-stopped
    ports:
      - "13492:13492"
    environment:
      - TESTING_API_KEY=${TESTING_API_KEY:-change-me}
    network_mode: host
